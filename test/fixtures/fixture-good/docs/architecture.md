# Arquitetura deste projeto de teste

Este código segue os princípios SOLID: responsabilidade única, aberto/fechado,
substituição de Liskov, segregação de interface e inversão de dependência.

Também usa alguns padrões de projeto (GoF), como Factory e Strategy, para
isolar a criação e a variação de comportamento entre as camadas.
